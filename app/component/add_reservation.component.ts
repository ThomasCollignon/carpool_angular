import { Component } from '@angular/core';

@Component({
    // selector for tag in index.html
    selector: 'addreservation',
    // to use relative urls
    moduleId: module.id,
    templateUrl: '../html/add_reservation.component.html',
    styleUrls: ['../css/add_reservation.component.css']
})
export class AddReservationComponent{}
